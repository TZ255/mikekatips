<!-- Variables -->
<% title = 'Ingia - MikekaTips' %>
<% description = 'Ingia kwenye akaunti yako ya MikekaTips ili kupata tips za kulipwa na za hali ya juu.' %>
<% keywords = 'login, ingia, akaunti, mikekatips, betting tips' %>
<% page = 'auth' %>
<% page_url = '' %>

<main>
    <div class="container py-5" id="tgt-regcontainer">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="card shadow-lg border-0 rounded-4">
                    <div class="card-body p-5">
                        <!-- Header -->
                        <div class="text-center mb-4">
                            <div class="bg-primary bg-opacity-10 rounded-circle d-inline-flex p-3 mb-3">
                                <i class="fa-solid fa-circle-user display-6 text-primary"></i>
                            </div>
                            <h2 class="fw-bold text-dark mb-2">Karibu Mdau!</h2>
                            <p class="text-muted">Ingia kwenye akaunti yako ili kuona premium tips zinazoshinda.</p>
                        </div>

                        <!-- Flash Messages -->
                        <%- include('../0-partials/flashMessages') %>
                        <!-- Google Sign In Button -->
                        <div class="d-grid gap-3">
                            <a href="/auth/google" class="btn btn-danger btn-lg py-3 rounded-3 shadow-sm" id="googleSignInBtn">
                                <i class="fa-brands fa-google me-2"></i>
                                <span>Ingia kwa Google</span>
                            </a>
                        </div>

                        <!-- Legal Links -->
                        <div class="text-center mt-3">
                            <p class="small text-muted mb-0">
                                <a href="/terms-of-service" class="text-muted text-decoration-none" target="_blank">
                                    Masharti ya Huduma
                                </a> 
                                | 
                                <a href="/privacy-policy" class="text-muted text-decoration-none" target="_blank">
                                    Sera ya Faragha
                                </a>
                            </p>
                        </div>

                        <!-- Back to Home -->
                        <div class="text-center mt-4">
                            <a href="/" class="btn btn-outline-secondary btn-sm rounded-pill px-4">
                                <i class="fa-solid fa-arrow-left me-1"></i>
                                Rudi Nyumbani
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

<!-- Server-side Google OAuth redirect; no client Firebase needed -->
